# Private

`The Archive`를 위해 직접 제작한 Hugo 테마입니다. 다른 테마를 포팅한 것이 아니며, 별도의 범용 테마나 독립 패키지로 배포하는 것을 전제로 하지 않습니다.

사이트 설정과 메뉴는 저장소 루트의 `config.toml`에서 관리합니다. 테마 디렉터리의 `hugo.toml`에는 독립 실행 시 필요한 최소 Hugo 버전만 기록합니다.

## Development

저장소 루트에서 다음 명령으로 확인합니다.

```shell
hugo server -D
```
